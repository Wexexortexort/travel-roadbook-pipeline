# Changelog

本项目的所有重要变更记录于此。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- **Phase 0 新增第 13 项「设计倾向」采集**；**Phase 4 新增 §4.0「设计方向确认」**——
  日卡形态与配色方向改由用户从**可视化预览**中选定，不设默认值
- **Phase 6 新增 §6.0「需求对照」**——交付前逐项回对需求卡，防止设计方向被默认值带过

### Changed

- **依赖改为三档**：仅 Python 3.10+ 为必需；**高德 REST Key 与浏览器自动化工具降为「建议项，可跳过」**，
  缺任一项都照常开工，走备选方案（OSRM 里程 / 用户贴攻略链接），交付时标注 ⏳ 待核实
- **浏览器自动化工具不再锁死实现**——bsk / browser-use / Agent-Browser / Playwright 任选其一；
  新增 `BROWSER_TOOL_PATH` 环境变量（旧名 `BSK_PATH` 仍兼容）。技能只要求三项能力：
  打开页面 / 取整页 HTML / 在页面上下文执行 JS（且能复用登录态）
- `preflight.py` 输出改为三类结论：✅ 全部依赖就位 / 🟡 可以开工（降级模式）/ ❌ 缺少必需项

### Fixed

- **修复「设计方向」指令悬空**：文档要求「由用户在 Phase 0 选定」，但采集清单里没有这项，
  同时摘要表与规范正文又给出了确切默认值（铁路图 / 暖沙纸），导致 agent 直接套用默认值而不询问。
  现已全链路删除默认值表述，并把要求落到可执行的采集项（Phase 0）与确认步骤（Phase 4 §4.0）上
- **`preflight.py` 输出可读性**：安装指引里的 Python 路径此前被兜底扫描打成
  `<redacted:win_user_path>\...`，那条命令既看不懂也没法直接复制；现改走脱敏（`<home>\...`）并注明如何替换。
  另：「⚪ 可选」与「🟡 建议」此前显示为同一个标记，现改为**三档三标记**

## [1.0.0] - 2026-09-19

首个公开发布版本（项目名：**路书生成器**）。

### Added

- **端到端编排链**：Phase -1（环境预检）→ 0（交互式需求采集）→ 1（路线规划）→
  2（多渠道交叉验证）→ 3（内容规划）→ 4（HTML 生成）→ 5（图片补充与校验）→ 6（交付前自检）
- **实时取数优先**：路线与里程走高德 REST 实时导航数据；在地情报走小红书实时检索
- **`scripts/preflight.py`** — 环境依赖体检，含自包含的缺失指引；路径无关
  （环境变量 → 标准位置 → PATH 三级查找）
- **`scripts/imgverify.py`** — 分级闸门图片校验
  - `light`（默认，闸门 ①②）/ `full`（五道全跑）/ 编号串（如 `1245`）
  - 五道闸门：有效性 / 尺寸合理性 / 素材类型 / 语义一致性 / 命名互锁
- **`scripts/imgcrop.py`** — 3:4 裁切落盘、命名断言、用户手选图替换通道（命名约定驱动，零配置）
- **`references/roadbook-spec-v1.0.md`** — 通用路书制作规范（结构 / 视觉 / 内容三类约束）
- **`references/image-validation.md`** — 图片校验机制的设计说明与调参指南
- **小红书情报时效三筛**：2 年内发布 + 同季节 + 时效分水岭，递进式硬筛
- **单文件零外链 HTML 交付**：不需要部署、服务器或任何账号
- **跨 Agent 可用**：标准 SKILL.md 约定，无平台专有依赖
- 示例目录 `examples/`：可直接运行的候选清单与三档真实判定输出

### Changed

- 项目中文名定为「**路书生成器**」；README 增加「为什么比其他做法更可靠」章节，
  明确对比现查现用与模型记忆的差异
- 安装章节改为**通用安装**为主，各 Agent 技能目录以折叠表列出

### Fixed

- **修正高德 API 表述过度承诺**：不再把 `/v3/direction/driving` 称为"实时导航数据/实时路况"。
  按官方文档，`strategy=0` 的说明中**不含路况**，返回的 `distance`/`duration`/`tolls` 均为
  **查询时点估算**；官方也明说"间隔一段时间后相同起终点可能返回不同结果"。
  现统一表述为「查询时点的路线计算」，实时拥堵/施工/封路一律走官方交通源
- **`preflight.py` 输出脱敏**：默认不再输出依赖绝对路径（含用户名）、
  高德 Key 前缀、bsk 原始状态。新增 `--verbose` 按需输出诊断细节
- **图片改为纯图片、零跳转链接**：不再建议把带 `xsec_token` 的小红书链接写入产物。
  该参数有时效，写进对外页面会失效；`photos.json` 的 `xhs` 仅作内部留痕，不入 HTML

### 设计取舍

- 图片默认走**轻量档** —— 图片是加分项，不是核心交付物
- **不含部署/上线步骤** —— 交付物是本地文件
- OSRM **只用作里程交叉**，不作时长口径
- 无真实数据拟合的装饰性图形不绘制

[1.0.0]: https://github.com/Wexexortexort/travel-roadbook-pipeline/releases/tag/v1.0.0
